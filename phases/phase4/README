Στις φάσεις 4 και 5 ουσιαστικά συμπληρώνεται η κατασκευή του μεταγλωττιστή με την παραγωγή
τελικού κώδικα (φάση 4) ενώ ταυτόχρονα δημιουργούμε τον εξομοιωτή της μηχανής alpha (φάση 5)
και κατασκευάζουμε τις συναρτήσεις βιβλιοθήκης (με την υποχρεωτική υποστήριξη των
συναρτήσεων βιβλιοθήκης print, typeof, totalarguments και agrument).

Το πρώτο βήμα είναι η παραγωγή τελικού κώδικα. Παράγετε κώδικας τόσο
σε δυαδική όσο και σε μορφή κειμένου, ώστε να μπορούμε να ελέγχουμε την ορθότητά του τουλάχιστον
με οπτικό τρόπο. 
Το δεύτερο βήμα είναι η κατασκευή της εικονικής μηχανής που θα φορτώνει και θα
τρέχει τον κώδικα μηχανής. 